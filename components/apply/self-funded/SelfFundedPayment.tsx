import ApplyPaymentSection, {
  type ApplyPaymentSectionProps,
} from '@/components/apply/shared/ApplyPaymentSection';

export default function SelfFundedPaymentSection(props: ApplyPaymentSectionProps) {
  return <ApplyPaymentSection {...props} />;
}
