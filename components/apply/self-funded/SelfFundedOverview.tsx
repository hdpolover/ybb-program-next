import ApplyOverviewSection, {
  type ApplyOverviewSectionProps,
} from '@/components/apply/shared/ApplyOverviewSection';

export default function SelfFundedOverviewSection({ data }: ApplyOverviewSectionProps) {
  return <ApplyOverviewSection data={data} />;
}
